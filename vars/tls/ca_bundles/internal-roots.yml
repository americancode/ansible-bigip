---
# CA bundles may also use inline `!vault` when local policy requires encryption at rest.
tls_ca_bundles:
  - name: "internal_root_bundle"
    content: |
      -----BEGIN CERTIFICATE-----
      REPLACE_WITH_REAL_INTERNAL_CA_BUNDLE_CONTENT
      -----END CERTIFICATE-----
    true_names: true
